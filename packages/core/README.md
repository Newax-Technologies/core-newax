# Core Package

This package is reserved for shared NEWAX Core foundations.

It may later contain common domain primitives, shared contracts, cross-cutting abstractions, and platform-level concepts that are intentionally reused by multiple modules.

No production code has been added yet.
