# Runtime environment
NODE_ENV=development

# Network binding
HOST=0.0.0.0
PORT=3000

# PostgreSQL connection used by Prisma and the API data layer.
# Replace these local development credentials for each deployment.
DATABASE_URL=postgresql://newax:newax@localhost:5432/newax?schema=public
