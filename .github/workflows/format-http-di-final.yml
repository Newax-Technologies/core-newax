name: Format HTTP DI Final

on:
  push:
    branches:
      - agent/http-security-boundary

permissions:
  contents: write

jobs:
  format:
    if: ${{ !contains(github.event.head_commit.message, '[skip http di formatter]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/http-security-boundary

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          package-manager-cache: false

      - run: pnpm install --frozen-lockfile

      - name: Apply repository formatter
        run: |
          pnpm exec prettier --write \
            apps/api/src/health/health.controller.spec.ts \
            apps/api/src/http-security/http-security.guard.ts \
            apps/api/src/http-security/http-security.interceptor.ts \
            apps/api/src/http-security/http-security-exception.filter.ts \
            apps/api/src/main.ts \
            packages/http-security/tsconfig.json \
            packages/http-security/src/services/request-origin-policy.ts

      - name: Commit formatted files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            apps/api/src/health/health.controller.spec.ts \
            apps/api/src/http-security/http-security.guard.ts \
            apps/api/src/http-security/http-security.interceptor.ts \
            apps/api/src/http-security/http-security-exception.filter.ts \
            apps/api/src/main.ts \
            packages/http-security/tsconfig.json \
            packages/http-security/src/services/request-origin-policy.ts

          if git diff --cached --quiet; then
            exit 0
          fi

          git commit -m "Format HTTP security dependency injection [skip http di formatter]"
          git push origin HEAD:agent/http-security-boundary
