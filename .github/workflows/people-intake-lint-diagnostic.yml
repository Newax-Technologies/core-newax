name: Capture People Intake Lint Diagnostics Temporary

on:
  pull_request:
    types:
      - synchronize
      - reopened

permissions:
  contents: read

jobs:
  lint:
    if: github.head_ref == 'agent/family-intake-verification-dashboard'
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out feature branch
        uses: actions/checkout@v7
        with:
          ref: agent/family-intake-verification-dashboard
          fetch-depth: 1
          persist-credentials: false

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          package-manager-cache: false

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Capture lint output
        run: |
          set +e
          pnpm lint > /tmp/people-intake-lint.log 2>&1
          status=$?
          set -e
          cat /tmp/people-intake-lint.log
          echo "exit_status=${status}" >> /tmp/people-intake-lint.log

      - name: Upload lint diagnostic
        uses: actions/upload-artifact@v7
        with:
          name: people-intake-lint-${{ github.sha }}
          path: /tmp/people-intake-lint.log
          if-no-files-found: error
          retention-days: 1
