name: Format Authentication HTTP Logout

on:
  push:
    branches:
      - agent/authentication-http-endpoints

permissions:
  contents: write

jobs:
  format:
    if: ${{ !contains(github.event.head_commit.message, '[skip authentication logout formatter]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/authentication-http-endpoints

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          package-manager-cache: false

      - run: pnpm install --frozen-lockfile

      - name: Format logout hardening
        run: |
          pnpm exec prettier --write \
            apps/api/src/authentication-http/authentication-http.controller.ts \
            apps/api/src/authentication-http/authentication-http.controller.spec.ts

      - name: Commit formatted files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            apps/api/src/authentication-http/authentication-http.controller.ts \
            apps/api/src/authentication-http/authentication-http.controller.spec.ts
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Format authentication logout hardening [skip authentication logout formatter]"
          git push origin HEAD:agent/authentication-http-endpoints
