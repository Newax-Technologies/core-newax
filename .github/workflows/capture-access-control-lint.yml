name: Capture Access Control Lint

on:
  push:
    branches:
      - agent/access-control-services

permissions:
  contents: write

jobs:
  capture:
    if: ${{ !contains(github.event.head_commit.message, '[skip access lint]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/access-control-services

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          package-manager-cache: false

      - run: pnpm install --frozen-lockfile

      - name: Capture lint diagnostics
        run: pnpm lint > access-control-lint-report.txt 2>&1 || true

      - name: Commit lint report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add access-control-lint-report.txt
          git commit -m "Capture access control lint diagnostics [skip access lint]"
          git push origin HEAD:agent/access-control-services
